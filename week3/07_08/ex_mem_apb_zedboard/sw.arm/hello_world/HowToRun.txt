
$ set_vivado 2020.2
< Make sure Platform Project (xsa) is ready by Vivado >
< Make sure 'design wrapper' is generated by Vitis >
< 1. select Tools->Launch Vitis IDE
  2. set workspace: ...../sw/workspace
  3. select "PROJECT->Create Application Project
  4. select tab: "Create a new platform from hardware (XSA)"
  5. select XSA: ...../hw/xsa/mem_preset_wrapper.xsa
  6. set application project name: mem_all
  7. select operating system: standalone
  8. select embedded software development template: 'Hello world'
  9. Right button on 'mem_all' menu and then select 'Build Project'
  11. Followings are used by application 
      "../workspace/mem_all/src/helloworld.c,lsscript.ld,...'
      "../workspace/mem_preset_wrapper/export/mem_preset_wrapper/sw/mem_preset_wrapper/standalone_microblaze_0/bspinclude/include"
      "../workspace/mem_preset_wrapper/export/mem_preset_wrapper/sw/mem_preset_wrapper/standalone_microblaze_0/bsplib/lib"
  12. XSDB script used
      "../workspace/mem_all_system/_ide/scripts/debugger_mem_all-default.tcl"

$ make

< Make sure open serial emulator (gtkterm) on /dev/ttyUSB1 and /dev/ttyUSB3 with 115200-8-N-1>
     $ sudo gtkterm -s 115200 -p /dev/ttyUSB3
< Turn on the board >
< Make sure the board passed 'System Controller Initialization Finished ...' on /dev/ttyUSB3 >
< Make sure the IPI Test on /dev/ttyUSB1 >
     This (/dev/ttyUSB1) will be the console for the program.

< Configure FPGA>
$ make configure

< It may take a few minutes. >
< Make sure the LED S13 - FPGA DONE lit >

< Download ELF>
$ make download
< You should see 'Hello World' message on /dev/ttyUSB1.>

