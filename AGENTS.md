# Codex 작업 및 인수인계 지침

이 파일은 계정이나 대화가 바뀌어도 프로젝트 작업 방식을 유지하기 위한 최상위 지침이다.

## 새 작업 시작 순서

1. `CONTEXT.md`를 읽어 프로젝트의 목적과 구성을 파악한다.
2. `DECISIONS.md`를 읽어 이미 합의한 결정과 이유를 확인한다.
3. `TODO.md`를 읽어 현재 상태와 다음 작업을 확인한다.
4. 작업 대상 하위 폴더에 별도의 `AGENTS.md`가 있으면 그 지침도 적용한다.
5. 실제 파일과 문서가 인수인계 내용보다 최신이면 실제 상태를 우선하고 차이를 알린다.

## 작업 원칙

- 사용자의 현재 요청을 최우선으로 따른다.
- 질문과 관련된 로컬 자료를 먼저 검색하고, 답변에는 근거가 된 파일 경로를 표시한다.
- 추측과 파일에서 확인한 사실을 명확히 구분한다.
- 기존 사용자 변경을 임의로 되돌리거나 정리하지 않는다.
- 현재 작업 트리에는 Vivado 등 도구가 만든 대규모 변경이 있을 수 있다. 요청 범위 밖의 생성물은 수정하지 않는다.
- 삭제, 대규모 이동, Git 이력 변경처럼 복구가 어려운 작업은 사용자 승인 없이 하지 않는다.
- `.env`, 인증 키, 토큰 및 개인정보의 실제 값을 인수인계 문서나 답변에 복사하지 않는다.
- 검증 명령은 작업 대상에 맞게 최소 범위로 실행하고, 실행하지 못한 검증은 명시한다.

## 인수인계 문서 갱신

의미 있는 작업이 끝나면 필요한 문서만 갱신한다.

- 장기적으로 유지할 배경이나 구조 변경: `CONTEXT.md`
- 확정된 선택과 그 이유: `DECISIONS.md`
- 완료 항목, 진행 중 작업, 다음 단계: `TODO.md`
- 저장소 전체에 계속 적용할 작업 규칙: `AGENTS.md`

대화 전문을 그대로 기록하지 말고, 다음 작업자가 판단하는 데 필요한 사실만 간결하게 남긴다.

## Google Drive 자료

- 자동 동기화 폴더의 권장 위치는 저장소 아래 `GoogleDrive/`이다.
- 폴더가 존재하면 질문과 관련된 자료를 우선 검색한다.
- 동기화 중이거나 온라인 전용인 파일은 내용이 아직 로컬에 없을 수 있으므로 접근 가능 여부를 확인한다.
- Google Drive 원본을 수정하거나 삭제하는 작업은 사용자가 명시적으로 요청한 경우에만 수행한다.

