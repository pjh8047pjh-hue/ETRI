# 입출력 스펙

## weight 
weight 값은 16bit로 받아서 ROM에 저장해놓는다. 384번의 clk이 지나면 새로운 input이 들어올 것이기 때문에 계산될 clk을 계산해서 다음 weight를 불러온다

## input
layer 07의 output을 입력으로 받는다. 이는 

## ouput
weight가 업데이트 되는 조건과 마찬가지다. input을 바꿔가면서 계산한다 32bit로 값이 누적되면 BRAM에 업데이트 한다.
--> 값이 32bit가 나오더라도 다음 layer의 입력을 생각해서 정수부를 누적할 것인지 소수점 아래를 자를 것인지를 고려해야한다.


# pointwise 연산

## input [64][14][14]
이 때 한번에 불러오는 값은
[0][0][0]에 weight [0] -> [1][0][0] -> [2][0][0] -> ... [63][0][0]